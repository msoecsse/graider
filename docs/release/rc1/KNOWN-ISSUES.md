# Graider RC1 Known Issues

- Student report publishing is not yet in the UI.
- Workflow generation UI is deferred.
- Private GitHub links may require signing into GitHub in the browser.
- GitHub CLI authentication is required for repository checks and grading
  actions.
- The macOS app is unsigned and may show macOS security warnings.
- Code signing and notarization are deferred.
- Windows builds are not validated.
- Linux builds are not validated.
- Auto-update is not configured.
- The app uses the default Electron icon.
